<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="0dp"
    android:layout_height="fill_parent"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:gravity="center_horizontal"
    android:orientation="horizontal">

    <ImageButton
        android:id="@+id/choice_button"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="1"
        android:background="@color/transparent"
        android:cropToPadding="true"
        android:scaleType="center"
        android:src="@drawable/icon_choice_disabled"
        android:text="@string/questions_button" />

    <ImageButton
        android:id="@+id/new_choice_button"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="1"
        android:background="@color/transparent"
        android:cropToPadding="true"
        android:scaleType="center"
        android:src="@drawable/icon_new_choice_disabled"
        android:text="@string/add_question_button" />

    <ImageButton
        android:id="@+id/profile_button"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="1"
        android:background="@color/transparent"
        android:cropToPadding="true"
        android:scaleType="center"
        android:src="@drawable/icon_profile_disabled"
        android:text="@string/settings_button" />
</LinearLayout>