<resources>

    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/transparent</item>
        <item name="android:statusBarColor">@color/gradient_start</item>
    </style>

    <style name="CommentTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/transparent</item>
        <item name="android:statusBarColor">@color/comments_background</item>
    </style>

    <style name="TabsTheme" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/ubuntu_regular</item>
    </style>
</resources>
